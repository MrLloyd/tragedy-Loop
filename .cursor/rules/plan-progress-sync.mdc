---
description: 每完成一步实现任务，同步更新 PLAN.md（及必要时 PLAN_CODE_MAPPING.md）
alwaysApply: true
---

# 计划与映射文档同步

本仓库的单一进度源是 **[`PLAN.md`](../../PLAN.md)**（见其中 **「📋 文档更新规则」**）。另：若改动涉及「计划条目 ↔ 代码路径」对应关系，同步维护 **[`PLAN_CODE_MAPPING.md`](../../PLAN_CODE_MAPPING.md)**。

## 何时更新

在**结束一项开发步骤或子任务时**（同一轮对话内或同一 PR/提交前），必须完成文档对齐，再视为该步骤收尾：

1. **勾选与文案**：将对应 `- [ ]` 改为 `- [x]`（或保持未完成并写明阻塞原因）；必要时补充日期戳（如 `✅ 2026-04-18`），与 `PLAN.md` 现有风格一致。
2. **范围**：只改**与本次变更直接相关**的条目与子章节；不要批量勾选无关阶段。
3. **偏差**：若实现与 `PLAN.md` 原表述不一致，在**紧邻条目**或「实施进度」小节中简述**差异与原因**。
4. **部分完成**：未做完的工作保持 `- [ ]`，并写清**剩余项**（避免误标完成）。

## 禁止

- 只改代码、不更新 `PLAN.md` 中应对应的进度（当该任务本可映射到计划条目时）。
- 在 `PLAN.md` 中做与本次任务无关的大段重写或清理。
