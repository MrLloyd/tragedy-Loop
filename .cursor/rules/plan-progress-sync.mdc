---
description: 完成实现后同步更新 PLAN.md 完成状态
alwaysApply: true
---

# 计划进度同步

每次完成一个开发任务后，必须在 `PLAN.md` 中同步更新对应条目的完成情况（如 `- [ ]` -> `- [x]`，或补充「已完成/进行中/阻塞」说明）。

要求：

1. 只更新与本次任务直接相关的条目，不要批量改动无关阶段。
2. 若实现与原计划有偏差，在 `PLAN.md` 对应位置写明差异与原因。
3. 若任务未完全完成，保持未完成状态并注明剩余工作，避免误标完成。
